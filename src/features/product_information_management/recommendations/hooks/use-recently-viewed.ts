"use client";
const KEY = "ls_recent_products";
export function use_recently_viewed() {
  // read/write JSON array of { product_id, viewed_at }
  // POST /api/storefront/recommendations/view on PDP mount
}

// TODO
// src/app/[locale]/c/[category_slug]/page.tsx — same client with category_slug from params + generateMetadata using category name (fetch category by slug server-side).

// Canonical URL example:
// /fr/c/patisseries?price_min=500&in_stock=1
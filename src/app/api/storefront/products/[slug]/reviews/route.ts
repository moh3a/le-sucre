// TODO
// GET /api/storefront/products/[slug]/reviews → resolve product by slug, return summary + paginated approved reviews
// POST /api/storefront/products/[slug]/reviews → auth customer, create pending review
